$HTTP["url"] =~ "^/(dl_statistics|vc)" {
    fastcgi.server = (
            ".php" => ((
                            "bin-path" => "/usr/bin/php-cgi",
                            "socket" => "/tmp/php.socket",
                            "max-procs" => 1
                    ))
    )
}
